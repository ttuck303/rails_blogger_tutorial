<h1>Tags</h1>
<ul>
	<% @tags.each do |tag| %>
		<li><%= link_to tag.name, tag_path(tag) %>, <%= link_to "Delete Tag", tag_path(tag),method: :delete, data: { confirm: "Really delete the tag?"} %></li>
	<% end %>
</ul>
<%= link_to "<< Back to Articles List", articles_path %>

